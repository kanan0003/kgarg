<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".EditItemActivity" >

    <TextView
        android:id="@+id/tvEditItemID"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_marginLeft="14dp"
        android:text="@string/tvEditItemLabel"
        android:textSize="20sp" />

    <EditText
        android:id="@+id/etEditItemID"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/tvEditItemID"
        android:layout_below="@+id/tvEditItemID"
        android:layout_marginTop="27dp"
        android:ems="10"
        android:hint="@string/etHintLabel" />

    <Button
        android:id="@+id/btnSaveID"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/etEditItemID"
        android:layout_below="@+id/etEditItemID"
        android:layout_marginTop="20dp"
        android:text="@string/btnSave"
        android:onClick="save" />

</RelativeLayout>
